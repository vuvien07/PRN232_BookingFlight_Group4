<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flight Booking</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="~/css/common/Footer.css">
    <style>
        body {
            background: #f8f9fa;
            position: relative;
            padding-top: 69px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .banner {
            position: relative;
            width: 100%;
            background: #fff;
        }

            .banner img {
                height: 440px;
                margin-top: -70px;
                width: 100%;
                object-fit: cover;
            }

        .promotion-section {
            margin-top: 50px;
        }

        .promo-card img {
            width: 100%;
        }

        /*Event Slideshow*/

        .event-slideshow {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 70%;
            max-width: 1100px;
            padding: 0;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }


        .event-wrapper {
            position: relative;
            width: 100%;
            height: 300px;
            overflow: hidden;
            border-radius: 0;
        }


        .event-slide {
            position: absolute;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 0.5s ease-in-out;
        }


            .event-slide.active {
                opacity: 1;
            }


            .event-slide img {
                width: 100%;
                height: 100%;
                object-fit: fill;
                display: block;
            }

        /* Navigation buttons */
        .slide-btn {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(0, 0, 0, 0.5);
            color: white;
            padding: 12px 18px;
            font-size: 18px;
            border: none;
            cursor: pointer;
            border-radius: 50%;
            transition: background 0.3s;
            z-index: 10;
        }

            .slide-btn:hover {
                background: rgba(0, 0, 0, 0.8);
            }

        .prev {
            left: 10px;
        }


        .next {
            right: 10px;
        }

        /* Dots */
        .dot-container {
            position: absolute;
            bottom: 15px;
            left: 0;
            right: 0;
            text-align: center;
            z-index: 10;
        }

        .dot {
            height: 10px;
            width: 10px;
            margin: 0 5px;
            background-color: #bbb;
            border-radius: 50%;
            display: inline-block;
            cursor: pointer;
            transition: background-color 0.3s;
        }

            .dot.active {
                background-color: #006994;
            }

    </style>
</head>
<body>
    <partial name="~/Views/Common/_Header.cshtml" />
    <header class="banner position-relative">
        <img src="images/background1.jpg" alt="Banner">
        <!-- Event Slideshow -->
        <div class="event-slideshow">
            <div class="event-container">
                <div class="event-wrapper" id="eventWrapper">
                    <div class="event-slide active">
                        <img src="~/images/event1.webp" alt="Event 1">
                    </div>
                    <div class="event-slide">
                        <img src="~/images/event2.jpg" alt="Event 2">
                    </div>
                    <div class="event-slide">
                        <img src="~/images/event3.png" alt="Event 3">
                    </div>
                </div>


                <button class="slide-btn prev" onclick="changeSlide(-1)">❮</button>
                <button class="slide-btn next" onclick="changeSlide(1)">❯</button>


                <div class="dot-container">
                    <span class="dot active" onclick="currentSlide(1)"></span>
                    <span class="dot" onclick="currentSlide(2)"></span>
                    <span class="dot" onclick="currentSlide(3)"></span>
                </div>
            </div>
        </div>
    </header>

    <div class="container">
        <div class="about-section promotion-section">
            <h4 class="mb-4">Về Chúng Tôi</h4>
            <div class="row align-items-center">
                <div class="col-md-6">
                    <div class="about-content">
                        <h5 class="mb-3" style="color: #006994;">Đối tác đáng tin cậy cho mọi hành trình của bạn</h5>
                        <p class="mb-4">
                            Chúng tôi tự hào là đơn vị tiên phong trong lĩnh vực đặt vé máy bay trực tuyến tại
                            Việt Nam, mang đến cho khách hàng những trải nghiệm đặt vé thuận tiện và an toàn nhất.
                        </p>
                        <div class="row g-4 mb-4">
                            <div class="col-6">
                                <div class="achievement-box p-3 text-center"
                                     style="background: #f8f9fa; border-radius: 8px;">
                                    <h3 style="color: #006994;">500K+</h3>
                                    <p class="mb-0">Khách hàng tin tưởng</p>
                                </div>
                            </div>
                            <div class="col-6">
                                <div class="achievement-box p-3 text-center"
                                     style="background: #f8f9fa; border-radius: 8px;">
                                    <h3 style="color: #006994;">50+</h3>
                                    <p class="mb-0">Đối tác hàng không</p>
                                </div>
                            </div>
                        </div>
                        <ul class="list-unstyled">
                            <li class="mb-2">✓ Đảm bảo giá tốt nhất</li>
                            <li class="mb-2">✓ Hỗ trợ 24/7</li>
                            <li class="mb-2">✓ Thanh toán an toàn</li>
                            <li>✓ Đặt vé nhanh chóng</li>
                        </ul>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="about-image"
                         style="border-radius: 10px; overflow: hidden; box-shadow: 0 0 20px rgba(0,0,0,0.1);">
                        <img src="~/images/about.jpg" alt="About Us" style="width: 100%; height: 400px; object-fit: cover;">
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="reviews-section promotion-section mb-5">
            <h4 class="mb-4">Khách hàng đánh giá những gì về dịch vụ của chúng tôi ?</h4>

            <div class="d-flex align-items-center mb-3">
                <div class="d-flex me-3">
                </div>
                <div>
                </div>

            </div>

            <div class="row">
            </div>

            <div class="text-center mt-3">
                <a href="/feedback" class="text-decoration-none">See List All Feedback</a>
            </div>
        </div>
    </div>

    <partial name="~/Views/Common/_Footer.cshtml" />

    <script>
        let currentSlideIndex = 1;
        const totalSlides = document.querySelectorAll('.event-slide').length;


        function changeSlide(direction) {
            showSlide(currentSlideIndex + direction);
        }


        function currentSlide(n) {
            showSlide(n);
        }


        function showSlide(n) {
            const slides = document.querySelectorAll('.event-slide');
            const dots = document.querySelectorAll('.dot');


            if (n > totalSlides) {
                currentSlideIndex = 1;
            } else if (n < 1) {
                currentSlideIndex = totalSlides;
            } else {
                currentSlideIndex = n;
            }


            // Hide all slides
            slides.forEach(slide => {
                slide.classList.remove('active');
            });


            // Remove active class from all dots
            dots.forEach(dot => {
                dot.classList.remove('active');
            });


            // Show current slide
            slides[currentSlideIndex - 1].classList.add('active');
            dots[currentSlideIndex - 1].classList.add('active');
        }


        // Auto slide
        setInterval(() => {
            changeSlide(1);
        }, 3000);


        // Initial call
        showSlide(currentSlideIndex);

    </script>

</body>

</html>
